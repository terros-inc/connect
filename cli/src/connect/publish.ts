import { TerrosClient } from '@terros-inc/sdk'

export async function publishConnectScripts(): Promise<void> {
  const client = new TerrosClient()
// await client.connect.version.add({versionData: {}})
//
//   await client.connect.script.add({})

}
